environment:
  variables:
    - name: NVIDIA_LLM_URL
      value: "http://host.docker.internal:8000/v1"
    - name: NVIDIA_EMBED_URL
      value: "http://host.docker.internal:8001/v1"
    # Since you're on Spark, ensure the key is passed for local auth
    - name: NGC_API_KEY
      value: "your_ngc_key_here"
